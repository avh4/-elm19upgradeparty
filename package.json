{
  "devDependencies": {
    "chokidar-cli": "^1.2.1",
    "mustache": "^3.0.1"
  },
  "scripts": {
    "build": "mustache -p scenes/charity.mustache -p scenes/head.mustache scenes/data.json scenes/3-coding.html.mustache > scenes/3-coding.html",
    "watch": "chokidar 'scenes/data.json' 'scenes/*.mustache' -c 'npm run-script build'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
