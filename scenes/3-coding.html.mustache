<!DOCTYPE html>
<html lang="en-US"">
  <head>
    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="theme-color" content="#157878">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="./scenes.css">
  </head>
  <body>
    <main class="main-scene" role="main">
      <div class="placeholder positioned" style="width: 960px; height: 720px;"></div>
      <div class="positioned" style="left: 960px; height: 100%; right: 0">
        <div class="placeholder positioned" style="width: 100%; height: 240px;">cam</div>
        <div class="positioned fade-bottom" style="top: 240px; width: 100%; height: 380px; padding: 10px">
          <div style="font-size: 24px; text-align: center">{{title}}</div>
          <p>{{summary}}</p>

          {{#schedule}}
            <h3>Schedule</h3>

            <ul class="schedule">
              {{#upcoming}}
                <li>{{description}}</li>
              {{/upcoming}}
            </ul>
          {{/schedule}}
        </div>

        {{#charity}}
          <div class="positioned" style="bottom: 0; height: 80px; font-size: 20px; padding: 10px">
            <span class="dim">Today's charity:</span> {{name}}<br/>
            <span class="dim">Donate at</dim> <a>{{donateLink}}</a>
          </div>
        {{/charity}}
      </div>
    </main>
  </body>
</html>
